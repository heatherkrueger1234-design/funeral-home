{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "adopt-legacy-data": "tsx ./src/adopt-legacy-data.ts",
    "encrypt-existing-documents": "tsx ./src/encrypt-existing-documents.ts",
    "generate-encryption-key": "node -e \"console.log(require('crypto').randomBytes(32).toString('base64'))\"",
    "backup-database": "tsx ./src/backup-database.ts",
    "accounts": "tsx ./src/accounts.ts",
    "stats": "tsx ./src/stats.ts"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "@types/pg": "^8.18.0"
  },
  "dependencies": {
    "pg": "^8.20.0",
    "@workspace/db": "workspace:*"
  }
}
