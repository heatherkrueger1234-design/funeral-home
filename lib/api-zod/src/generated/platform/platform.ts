/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * Holding Today for funeral homes - the calm, private portal a home gives a
family, and the console the director works it from.

Two audiences, two authentication schemes, and the split is load-bearing:

- **Staff** sign in and carry a session cookie. Their funeral home is read
  off their user row; a home id arriving in a request is never trusted.
- **Families** have no account. They arrive through a link that was texted
  to them and send its token as `Authorization: Bearer <token>`. Every one
  of their endpoints lives under `/family` and is scoped to the single
  case that token belongs to - there is no case id in any of those paths,
  because there is no case they could ask for other than their own.

 * OpenAPI spec version: 0.1.0
 */
import * as zod from "zod";

/**
 * A different credential, a different cookie and a shorter session than
a director's. This account can read across every funeral home in the
system, so a laptop left open should stop being a problem the same day
rather than the same month.

 * @summary Sign in to the platform console
 */
export const PlatformLoginBody = zod.object({
  email: zod.string(),
  password: zod.string(),
});

export const PlatformLoginResponse = zod
  .object({
    id: zod.number(),
    email: zod.string(),
    displayName: zod.string().nullable(),
    role: zod
      .enum(["owner", "support"])
      .describe(
        "`support` can look but not change. It exists so that helping a\nfuneral home does not require the ability to suspend one.\n",
      ),
    hasPassword: zod.boolean(),
    lastSeenAt: zod.date().nullable(),
    createdAt: zod.date(),
  })
  .describe(
    "One of us. Never a funeral home's staff member — a different table, a\ndifferent session and a different cookie, so that neither resolver can\never return the other kind of account.\n",
  );

/**
 * @summary Who is signed in to the platform console
 */
export const GetPlatformMeResponse = zod
  .object({
    id: zod.number(),
    email: zod.string(),
    displayName: zod.string().nullable(),
    role: zod
      .enum(["owner", "support"])
      .describe(
        "`support` can look but not change. It exists so that helping a\nfuneral home does not require the ability to suspend one.\n",
      ),
    hasPassword: zod.boolean(),
    lastSeenAt: zod.date().nullable(),
    createdAt: zod.date(),
  })
  .describe(
    "One of us. Never a funeral home's staff member — a different table, a\ndifferent session and a different cookie, so that neither resolver can\never return the other kind of account.\n",
  );
